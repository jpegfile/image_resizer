image resizer and background remover to generate football manager pictures

needs python 3.10+

resize default is 800x800

just unzip and run **launch_app.bat**

permission to use whereever and change whatever
